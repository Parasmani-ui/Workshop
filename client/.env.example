# Render server URL.
# Leave empty in local dev — Vite proxy handles /api and /socket.io.
# In production (Vercel), set this to your Render server URL, e.g.:
#   VITE_API_URL=https://chanakya-sim-server.onrender.com
VITE_API_URL=
